#!/usr/bin/env bash
# SessionStart hook: remind Claude to invoke cos-welcome skill
echo "IMPORTANT: You are starting a new session in CoS root. Invoke the cos-welcome skill immediately to show the startup menu before doing anything else."
